apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smart-media.fullname" . }}-test-connection"
  labels:
    {{- include "smart-media.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smart-media.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
